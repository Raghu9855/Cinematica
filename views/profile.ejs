<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Account</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #18122B;
            font-family: 'Poppins', sans-serif;
        }
        .dashboard-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            justify-content: center;
        }
        .profile-card, .subscription-card, .actions-card {
            width: 100%;
            max-width: 400px;
            background: linear-gradient(145deg, #F6FAB9, #F6FAB9);
            /* box-shadow: 5px 5px 15px rgba(0, 0, 0, 0.1), 5px -5px rgba(255, 255, 255, 0.7); */
            border-radius: 20px;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .profile-card:hover, .subscription-card:hover, .actions-card:hover {
            transform: scale(1.05);
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.2);
        }
        .profile-card img {
            width: 120px;
            height: 120px;
            object-fit: cover;
            border: 5px solid black;
            border-radius: 50%;
            margin-top: -60px;
        }
        .card-header {
            background: black;
            color: #F6FAB9;
            padding: 20px;
            text-align: center;
        }
        .card-body {
            padding: 20px;
            text-align: center;
            background-color: #F6FAB9;
        }
        .card-body p {
            margin: 10px 0;
        }
        .btn-primary, .btn-secondary, .btn-outline-danger {
            border-radius: 50px;
        }
        ul.action-list {
            list-style: none;
            padding: 0;
            text-align: center;
        }
        ul.action-list li {
            padding: 10px;
            margin: 5px 0;
            background: #F6FAB9;
            border-radius: 10px;
            transition: background 0.3s ease;
        }
        ul.action-list li:hover {
            background: black;
            color: #F6FAB9;
        }
        ul.action-list li a {
            text-decoration: none;
            color: inherit;
        }
    </style>
</head>
<body>
    <div class="container my-5">
        <h2 class="text-center mb-4 fw-bold " style="color: #F6FAB9;">My Account</h2>
        <div class="dashboard-container">
           
            <div class="profile-card">
                <div class="card-header">
                    <h4>Hi, <%= user.username %></h4>
                </div>
                <div class="card-body text-center">
                    <img src="/images/apic.png" alt="User Profile">
                
                    <h5 class="mt-3"><%= user.username %></h5>
                    <p>Email: <%= user.email %></p>
                
                    <!-- Form to Upload Profile Picture -->
                    
                
                    <!-- Edit Profile Form -->
                    <form action="/update-profile" method="POST" class="mt-3">
                        <div class="form-group">
                            <label for="username">Update Username</label>
                            <input type="text" class="form-control" id="username" name="username" placeholder="Enter new username" value="<%= user.username %>" required>
                        </div>
                        <input type="hidden" name="email" value="<%= user.email %>">
                        <button type="submit" class="btn btn-dark btn-sm mt-2">Save Changes</button>
                    </form>
                </div>
                
            </div>





            <!-- Subscription Card -->
            <div class="subscription-card">
                <div class="card-header">
                    <h4>Subscription Details</h4>
                </div>
                <div class="card-body">
                    <p><strong>Plan:</strong> Premium</p>
                    <p><strong>Start Date:</strong> 01-Jan-2024</p>
                    <p><strong>Expiry Date:</strong> 01-Jan-2025</p>
                    <div class="d-flex justify-content-center gap-2 mt-3">
                        <a href="#" class="btn btn-dark btn-sm">Upgrade Plan</a>
                        <a href="#" class="btn btn-outline-danger btn-sm">Cancel Subscription</a>
                    </div>
                </div>
            </div>

            <!-- Actions Card -->
            <div class="actions-card">
                <div class="card-header">
                    <h4>Quick Actions</h4>
                </div>
                <div class="card-body">
                    <ul class="action-list">
                        <li><a href="/fpassword">Change Password</a></li>
                        <li><a id="logoutButton" href="/logout">Log Out</a></li>
                        <a href="/deleteaccount" class="btn btn-danger">Delete Account</a>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
