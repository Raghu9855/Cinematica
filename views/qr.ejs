<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Code Generator</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Fonts for more styling -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">

    <!-- Custom CSS for styling -->
    <style>
        body {
            background: linear-gradient(120deg, #ff7e5f, #feb47b);
            font-family: 'Roboto', sans-serif;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            width: 350px;
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 15px 25px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 35px rgba(0, 0, 0, 0.15);
        }

        .card-header {
            background-color: #ff7e5f;
            color: white;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            padding: 20px;
            text-align: center;
        }

        .card-body {
            padding: 15px;
            text-align: center;
        }

        .qr-img {
            border-radius: 12px;
            border: 5px solid #ff7e5f;
            transition: transform 0.3s ease;
        }

        .qr-img:hover {
            transform: scale(1.05);
        }

        .card-footer {
            background-color: #feb47b;
            color: white;
            padding: 10px;
            border-bottom-left-radius: 15px;
            border-bottom-right-radius: 15px;
            text-align: center;
        }

        .btn-custom {
            background-color: #ff7e5f;
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 30px;
            transition: background-color 0.3s ease;
        }

        .btn-custom:hover {
            background-color: #feb47b;
        }
    </style>
</head>

<body>
    <div class="card shadow-lg">
        <div class="card-header">
            <h5 class="mb-0">Scan the QR Code</h5>
        </div>
        <div class="card-body">
            <p class="text-muted">Scan this to visit the website:</p>
            <img src="/images/qr_img.jpg" alt="QR Code" class="img-fluid qr-img">
        </div>
        <div class="card-footer">
            <a href="/login" class="btn btn-custom" >Visit Website</a>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
